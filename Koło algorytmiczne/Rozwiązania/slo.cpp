#include <iostream>
#include <vector>

int main()
{
    unsigned int a, b, n;

    std::cin >> n >> a >> b;

    std::vector<bool> s;
    for (int i = 1; i < n; i++)
    {
        //std::cin >> s[i];
    }
}