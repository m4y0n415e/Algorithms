#include <iostream>
#include <unordered_map>

int reduct(int, std::unordered_map<int, int> f);

int main()
{
    int n;
    
    std::cin >> n;

    std::unordered_map<int, int> fractions;

    fractions[0] = 1;
    for(int i{1}; i < n; i++)
    {
        int temp = n - 1;
        if(temp = 2)
        {
            //for(int i; i )
        }
        //fractions[i] = 

    }
}

int reduct(int, std::unordered_map<int, int> f);