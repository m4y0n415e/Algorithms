#include <iostream>
#include <vector>
#include <set>
#include <string>

using namespace std;

int main()
{
	int n;

	vector<vector<string>> nazw(n);

	string nazwisko1, nazwisko2;

	for (int i{}; i < n; i++)
	{
		cin >> nazwisko1 >> nazwisko2;

		nazw[i].push_back(nazwisko1);
	}

	int k{};
	while (k < n)
	{

	}
}